# To Do:

1. Make water Archimedes screw  and pipe craftable using the same recipe as vanilla, except using the lead chute (pipe) section, and specifically lead plates. Any hammer and shears will do.
2. Add a compatibility patch to work with the waterfall mod
3. Let the mod generate the config in ModConfig and use the values from that config
